{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Detector based on Kolmogorov-Smnirnov test",
  "technique": "Enumeration Testing",
  "technique_description": "Enumerate the first word in trigger",
  "technique_changes": "First try",
  "technique_type": ["Enumeration Testing"],
  "commit_id": "24c2cd684febd3ab0bf2230e2031f1cc15ce6194",
  "repo_name": "https://github.com/TDteach/TrojAI-Competition.git",
  "required": [],
  "additionalProperties": false,
  "type": "object",
  "properties": {
    "random_seed": {
		"description": "Random state seed during training.",
		"type": "integer",
		"minimum": 1,
		"maximum": 99999999,
		"suggested_minimum": 1234,
		"suggested_maximum": 12345
	},
  "generate_length": {
		"description": "Length to generate.",
		"type": "integer",
		"minimum": 5,
		"maximum": 10,
		"suggested_minimum": 5,
		"suggested_maximum": 10
	},
  "topk": {
		"description": "TopK predicted tokens will be considered.",
		"type": "integer",
		"minimum": 1,
		"maximum": 10,
		"suggested_minimum": 1,
		"suggested_maximum": 10
	},
  "topk_threshold": {
		"description": "At least this percentage of tested samples should contain the specific token within the topk predictions.",
		"type": "number",
		"minimum": 0.5,
		"maximum": 1,
		"suggested_minimum": 0.5,
		"suggested_maximum": 1
	},
  "detect_threshold": {
		"description": "The threshold used by final detection.",
		"type": "number",
		"minimum": 0.5,
		"maximum": 1,
		"suggested_minimum": 0.5,
		"suggested_maximum": 1
	}
  }
}
